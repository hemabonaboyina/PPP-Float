You are now implementing the ACTUAL reduction.

This is no longer analysis.

This is a CONTROLLED SURGICAL EXTRACTION of a scientifically-valid float PPP baseline from the current codebase.

# FILES

SOURCE:

* ppp_gps_glabforensic.py
* ambiguity_manager.py
* rebirth_forensic.py
* kf.py

TARGET:

* ppp_core_baseline.py

# PRIMARY OBJECTIVE

Create a reduced baseline PPP engine by REMOVING:

* forensic instrumentation
* dormant experiments
* duplicate diagnostics
* dead EKF variants
* lifecycle scaffolding
* audit CSV infrastructure
* print-heavy debug systems

while PRESERVING:

* validated PPP physics
* validated float ambiguity behavior
* validated EKF behavior
* validated observation equations
* validated convergence behavior

IMPORTANT:
This is NOT a rewrite.
This is NOT an optimization pass.
This is NOT an architecture redesign.

This is CONTROLLED TEXT SURGERY.

# CRITICAL RULES

1. NEVER modify the original source file.
2. Work ONLY inside:
   ppp_core_baseline.py
3. Preserve runtime execution order exactly.
4. Preserve:

   * x ordering
   * P ordering
   * ambiguity slot ordering
   * EKF call ordering
5. DO NOT touch _proc() logic initially.
6. DO NOT redesign EKF math.
7. DO NOT rewrite ambiguity logic.
8. DO NOT alter observation equations.
9. DO NOT change plotting outputs yet.
10. After EACH phase:

    * run engine
    * compare outputs
    * confirm no numerical drift.

# BASELINE DEFINITION

KEEP:

* GPS-only float PPP
* IF code/phase
* SP3/CLK/BIA/ATX parsing
* APC corrections
* relativity
* earth rotation
* Saastamoinen + GMF
* float ambiguities
* filter_standard()
* _joseph_symmetrise()
* MW confirmation gate
* startup clock limiter
* ZWD clamp
* post-fit ambiguity birth
* L2W-only phase lock

REMOVE:

* forensic instrumentation
* audit CSVs
* lifecycle scaffolding
* experiment accumulators
* dead EKF variants
* dispatch wrappers
* dormant VB/SAGE filters
* zombie-soften experiments
* Stage-2A/Stage-7 scaffolding
* PATCH 1/2/4/5 print systems
* ASSERT-1..4 hard aborts
* RebirthForensic

IMPORTANT:
DO NOT remove the actual validated heuristics.
Only remove the scaffolding around them.

# IMPLEMENTATION PHASES

## PHASE A — FORENSIC REMOVAL ONLY

1. Create:
   ppp_core_baseline.py

2. Remove:

   * rebirth_forensic imports
   * *fmt** helper functions
   * forensic print blocks
   * ASSERT-1..4
   * forensic accumulators:
     *fa**,
     *rw**,
     *s2a**,
     *pf**,
     _clk_series,
     _x_post_prev,
     _P_post_prev
   * audit summary blocks
   * forensic CSV handles

3. DO NOT alter:

   * EKF flow
   * ambiguity logic
   * _proc()
   * observation equations

4. Validation after Phase A:
   Compare against original:

   * solution CSV
   * phase RMS
   * code RMS
   * ambiguity reset epochs
   * convergence time

Expected:
IDENTICAL results.

## PHASE B — FILTER CLEANUP

1. Delete:

   * _filter_joseph
   * _filter_dispatch
   * USE_JOSEPH_FORM
   * filter()
   * filter_vbakf
   * filter_sage_husa
   * _sage_beta
   * KFOPT_* constants
   * TDISTB_* tables

2. Replace ALL:
   _filter_dispatch(...)
   with:
   filter_standard(...)

3. KEEP:
   _joseph_symmetrise()

4. Validation:
   outputs must remain IDENTICAL.

## PHASE C — AMBIGUITY MANAGER CLEANUP

1. Remove lifecycle scaffolding:

   * AmbiguityLifecycleState enum
   * lineage/generation tracking
   * transition_state()
   * lifecycle trace CSVs
   * consistency assertions
   * genealogy helpers

2. KEEP:

   * slot ownership
   * ambiguity birth/reset
   * sigma tracking
   * observable/in-filter state
   * reset counts
   * acceptance counters

3. DO NOT alter:

   * sidx mapping
   * state_index ownership
   * slot allocation ordering

4. Validation:
   ambiguity reset epochs must match exactly.

## PHASE D — EXPERIMENT CLEANUP

1. Remove:

   * ENABLE_FREEZE_OBS block
   * Stage-2A scaffolding
   * Stage-7 POS-FLOOR scaffolding
   * PATCH 1/2/4/5 print systems
   * MEAS_AUDIT systems
   * GPS-L2-STAB audit CSV

2. KEEP:

   * MW confirmation gate
   * startup clock limiter (PATCH 3)
   * L2W lock enforcement
   * zombie detector
   * ZWD clamp

3. IMPORTANT:
   DO NOT hard-delete zombie logic yet.

Instead:

* remove zombie SOFTEN experiment
* preserve zombie RESET path
* preserve coherent-mode gate as optional flag

4. Convert hard aborts:
   sys.exit()
   into:
   print warning + continue

## PHASE E — OPTIONAL SYSTEM GATING

1. Make OPTIONAL:

   * RTS smoother
   * OTL corrections
   * WL/NL infrastructure
   * snapshot CSVs

2. Default:
   disabled or dormant.

3. DO NOT delete AR infrastructure yet.

# VALIDATION REQUIREMENTS

After EVERY phase:

Run BOTH:

* original engine
* ppp_core_baseline.py

Compare:

* ENU solution
* ambiguity reset epochs
* ambiguity sigma evolution
* phase RMS
* code RMS
* innovation norm
* ZWD evolution
* convergence time

# ACCEPTANCE THRESHOLDS

Position:

* < 1 mm RMS drift

Phase RMS:

* < 0.1 mm drift

Code RMS:

* < 1 mm drift

Convergence:

* < 1 epoch difference

Ambiguity resets:

* EXACT match

ZWD:

* < 2 mm drift

# IMPORTANT WARNINGS

1. DO NOT touch _proc() early.
2. DO NOT reorder EKF steps.
3. DO NOT resize core state ordering.
4. DO NOT move ambiguity birth before EKF update.
5. DO NOT remove _joseph_symmetrise().
6. DO NOT remove startup clock limiter initially.
7. DO NOT aggressively simplify ambiguity handling.
8. DO NOT trust comments labelled “temporary”.
9. Preserve sidx ↔ state_index synchronization exactly.
10. Preserve:
    Q propagation
    → _proc()
    → reset/slip
    → H assembly
    → filter_standard()
    → symmetrize
    → ambiguity birth

# FINAL TARGET

Produce:

* scientifically-valid float PPP
* minimal hidden state
* minimal experimental contamination
* reproducible behavior
* maintainable baseline
* ~2500–3500 line clean core

WITHOUT:

* rewriting the PPP engine
* changing validated physics
* changing EKF math
* introducing architecture churn

The goal is:
stable scientific baseline first,
future modularization later.
